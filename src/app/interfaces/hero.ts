/**
 * @author Youri Janssen
 * Represents a Hero object with an ID and a name.
 * */
export interface Hero {
  /** The unique identifier of the hero. */
  id: number;
  /** The name of the hero. */
  name: string;
}
